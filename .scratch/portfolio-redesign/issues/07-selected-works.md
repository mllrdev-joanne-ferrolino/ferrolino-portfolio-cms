# 07 — Selected Works

**What to build:** The Works section renders as a card grid from content: Voky AB (type "eCommerce Web App", tag "Professional Project", linking to the live site) plus placeholder cards (tag "Personal Project", `[TODO]` type) with gradient placeholders instead of images, and the trailing More projects card ("More projects coming soon...").

**Blocked by:** 03 — Content model and Hero

**Status:** ready-for-agent

- [ ] Every card's title, type, and tag comes from content
- [ ] Voky AB card links out to the live site and opens in a new tab
- [ ] Cards without an image render as soft gradient blocks, no broken images
- [ ] Grid is responsive: 2-3 columns on desktop, one column on mobile
- [ ] The More projects card renders with the star motif and the coming-soon copy
- [ ] `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass

# 06 — Experience timeline

**What to build:** The Experience section renders as a timeline from content: the real entry (Full-stack Software Developer, Miller Solutions Development AB, 2020 - Present) plus the placeholder second row so the layout can be judged. Vertical line, dots, position, company, date, optional description.

**Blocked by:** 03 — Content model and Hero

**Status:** ready-for-agent

- [ ] Two rows render in chronological order, all fields from content
- [ ] The date "2020 - Present" renders as a string as written
- [ ] Placeholder row is visibly marked `[TODO]` and is easy to replace from content
- [ ] Timeline looks correct with one row too (if the placeholder is removed later)
- [ ] `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass

# 05 — About, Specialty and Toolkit

**What to build:** The About section renders from content: the framed grayscale photo, name, location pin with Dumaguete City, and the bio (placeholder text is fine). Below it, the mockup's split block: Specialty on the left ("Software Development" in display type) and the Toolkit chip list on the right (C#/.NET, ASP.NET Core, Optimizely CMS, MSSQL, RabbitMQ, Visma, SQL, Vue/Nuxt, TypeScript, Azure Cloud, HTML/CSS/JS, E-commerce), with icons where good ones exist.

**Blocked by:** 03 — Content model and Hero

**Status:** ready-for-agent

- [ ] Photo, name, location, and bio all come from content
- [ ] Photo renders grayscale in a rounded frame, matching the mockup's treatment
- [ ] Specialty label renders in the display font in havelock blue
- [ ] Toolkit chips render one per row with icons where available, all from content, and reorderable in the content file
- [ ] Layout holds on mobile (single column where needed)
- [ ] `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass
